#!/usr/bin/env python
# configurations of Word2Vec word embedding on Wikipedia data

dset = 'word2vec'
data_type = 'text'
dims = [300]
metric = 'cosine'

# MySQL table schema
schema_meta = 'i, name'
schema_header = None
